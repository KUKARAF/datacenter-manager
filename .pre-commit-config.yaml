repos:
  - repo: local
    hooks:
      - id: update-version
        name: Update version (major.minor.gitshortsha)
        entry: python scripts/update_version.py
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-commit]
